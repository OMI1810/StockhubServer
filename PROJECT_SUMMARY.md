# Полное резюме функционала проекта Stockhub Server

## 📋 Общая информация

**Название проекта:** Stockhub Server  
**Технологии:** NestJS, Prisma ORM, PostgreSQL, Redis, Express Session  
**Назначение:** Backend API для системы управления складскими операциями и организациями

---

## 🏗️ Архитектура проекта

### Модули приложения:
1. **AuthModule** - Аутентификация и авторизация
2. **UserModule** - Управление пользователями
3. **OrganizationModule** - Управление организациями
4. **WarehouseModule** - Управление складами
5. **EmailConfirmationModule** - Подтверждение email
6. **MailModule** - Отправка email-уведомлений
7. **PrismaModule** - Работа с базой данных

---

## 🔐 1. Аутентификация и авторизация

### 1.1. Регистрация пользователя
**Эндпоинт:** `POST /auth/register`  
**Описание:** Регистрация нового пользователя в системе  
**Требует авторизации:** Нет

**Функционал:**
- Валидация данных (email, пароль, ФИО)
- Проверка уникальности email
- Хеширование пароля (argon2)
- Создание пользователя с ролью `REGULAR` по умолчанию
- Автоматическая отправка письма для подтверждения email
- Пользователь создается с `isVerified: false`

**Поля запроса:**
- `firstName` - Имя
- `lastName` - Фамилия
- `middleName` - Отчество (опционально)
- `email` - Email (уникальный)
- `password` - Пароль (минимум 8 символов)
- `passwordRepeat` - Подтверждение пароля

---

### 1.2. Подтверждение email (верификация)
**Эндпоинт:** `POST /auth/email-confirmation`  
**Описание:** Подтверждение email пользователя по токену  
**Требует авторизации:** Нет

**Функционал:**
- Проверка токена подтверждения
- Проверка срока действия токена (1 час)
- Активация пользователя (`isVerified: true`)
- Автоматическая авторизация после подтверждения (создание сессии)
- Удаление использованного токена

**Поля запроса:**
- `token` - Токен подтверждения (UUID)

**Особенности:**
- Токен отправляется на email пользователя после регистрации
- Токен действителен 1 час
- После подтверждения пользователь автоматически авторизуется

---

### 1.3. Авторизация (логин)
**Эндпоинт:** `POST /auth/login`  
**Описание:** Вход пользователя в систему  
**Требует авторизации:** Нет

**Функционал:**
- Проверка email и пароля
- Верификация пароля (argon2)
- Проверка подтверждения email
- Создание сессии пользователя (cookie-based)
- Сохранение `userId` в сессии

**Поля запроса:**
- `email` - Email пользователя
- `password` - Пароль (минимум 6 символов)

**Обработка ошибок:**
- Если email не подтвержден - отправляется новое письмо с токеном
- Если пароль неверный - возвращается ошибка 401
- Если пользователь не найден - возвращается ошибка 404

---

### 1.4. Выход (логаут)
**Эндпоинт:** `POST /auth/logout`  
**Описание:** Выход пользователя из системы  
**Требует авторизации:** Да

**Функционал:**
- Удаление сессии пользователя
- Очистка cookie сессии
- Сброс текущей выбранной организации

---

## 👤 2. Управление пользователями

### 2.1. Получение профиля текущего пользователя
**Эндпоинт:** `GET /users/profile`  
**Описание:** Получение информации о текущем авторизованном пользователе  
**Требует авторизации:** Да

**Возвращает:**
- `userId` - ID пользователя
- `email` - Email
- `firstName`, `lastName`, `middleName` - ФИО
- `role` - Роль пользователя (USER/ADMIN)
- `isVerified` - Статус подтверждения email
- `isTwoFactorEnabled` - Включена ли двухфакторная аутентификация
- `createdAt`, `updatedAt`, `lastLogin` - Даты

---

### 2.2. Получение пользователя по ID (только для админов)
**Эндпоинт:** `GET /users/by-id/:id`  
**Описание:** Получение информации о любом пользователе по ID  
**Требует авторизации:** Да  
**Требует роли:** `ADMIN` (системный администратор)

**Параметры URL:**
- `id` - ID пользователя (UUID)

---

## 🏢 3. Управление организациями

### 3.1. Получение списка организаций пользователя
**Эндпоинт:** `GET /organizations`  
**Описание:** Получение всех организаций, в которых участвует текущий пользователь  
**Требует авторизации:** Да

**Возвращает:**
Массив объектов с информацией об организациях:
- `organizationId` - ID организации
- `name` - Название организации
- `createdBy` - ID создателя
- `createdAt` - Дата создания
- `role` - Роль пользователя в организации (ADMIN/WAREHOUSE_WORKER)

---

### 3.2. Создание организации
**Эндпоинт:** `POST /organizations`  
**Описание:** Создание новой организации  
**Требует авторизации:** Да

**Функционал:**
- Создание организации с указанным названием
- Автоматическое добавление создателя в организацию с ролью `ADMIN`
- Транзакционное создание (организация + связь пользователя)

**Поля запроса:**
- `name` - Название организации (2-255 символов)

**Возвращает:**
- `organizationId` - ID созданной организации
- `name` - Название
- `createdBy` - ID создателя
- `createdAt` - Дата создания

---

### 3.3. Выбор текущей организации
**Эндпоинт:** `POST /organizations/:id/select`  
**Описание:** Выбор организации для работы (сохранение в сессии)  
**Требует авторизации:** Да  
**Требует участия в организации:** Да

**Функционал:**
- Проверка участия пользователя в организации
- Сохранение `organizationId` в сессии (`currentOrganizationId`)
- Использование выбранной организации для последующих операций

**Параметры URL:**
- `id` - ID организации (UUID)

**Важно:**
- После выбора организации, все операции (создание складов и т.д.) используют эту организацию из сессии
- Клиенту не нужно передавать `organizationId` в каждом запросе
- Организация сохраняется в cookie сессии

---

### 3.4. Получение текущей выбранной организации
**Эндпоинт:** `GET /organizations/current`  
**Описание:** Получение информации о текущей выбранной организации из сессии  
**Требует авторизации:** Да

**Возвращает:**
- Если организация выбрана:
  - `organizationId` - ID организации
  - `name` - Название
  - `role` - Роль пользователя в организации
- Если организация не выбрана:
  - `message: "Текущая организация не выбрана."`

---

### 3.5. Удаление организации
**Эндпоинт:** `DELETE /organizations/:id`  
**Описание:** Удаление организации  
**Требует авторизации:** Да  
**Требует роли:** `ADMIN` в организации

**Функционал:**
- Проверка прав доступа (только ADMIN может удалить)
- Проверка наличия связанных складов и товаров
- Транзакционное удаление (связи пользователей + организация)

**Параметры URL:**
- `id` - ID организации (UUID)

**Ограничения:**
- Нельзя удалить организацию, если есть связанные склады
- Нельзя удалить организацию, если есть связанные товары

---

## 📦 4. Управление складами

### 4.1. Создание склада
**Эндпоинт:** `POST /warehouses`  
**Описание:** Создание нового склада для текущей выбранной организации  
**Требует авторизации:** Да  
**Требует роли:** `ADMIN` в текущей организации

**Функционал:**
- Создание склада для организации из сессии (`currentOrganizationId`)
- Автоматическое присвоение статуса `ACTIVE`
- Валидация всех полей адреса

**Поля запроса:**
- `name` - Название склада (2-255 символов)
- `address` - Адрес (максимум 255 символов)
- `city` - Город (максимум 255 символов)
- `state` - Регион/штат (максимум 255 символов)
- `zip` - Почтовый индекс (максимум 255 символов)
- `country` - Страна (максимум 255 символов)

**Возвращает:**
- `warehouseId` - ID склада
- `organizationId` - ID организации
- Все поля склада
- `status` - Статус (ACTIVE/INACTIVE)
- `createdAt` - Дата создания

**Важно:**
- Перед созданием склада необходимо выбрать организацию через `POST /organizations/:id/select`
- Если организация не выбрана, вернется ошибка 400

---

## 🔒 5. Система авторизации и прав доступа

### 5.1. Guards (Охранники)

#### AuthGuard
**Назначение:** Проверка авторизации пользователя  
**Функционал:**
- Проверка наличия `userId` в сессии
- Извлечение информации о пользователе из базы данных
- Добавление объекта `user` в `request`

**Использование:**
- Декоратор `@Authorization()` применяет guard ко всем эндпоинтам, требующим авторизации

---

#### RolesGuard
**Назначение:** Проверка системных ролей пользователя  
**Функционал:**
- Проверка роли пользователя (USER/ADMIN)
- Сравнение с требуемыми ролями из декоратора

**Использование:**
- Декоратор `@Authorization(UserRole.ADMIN)` для эндпоинтов, доступных только администраторам

---

#### OrganizationRoleGuard
**Назначение:** Проверка участия и роли в организации  
**Функционал:**
- Получение `organizationId` из:
  1. Параметров URL (`request.params.organizationId` или `request.params.id`)
  2. Сессии (`request.session.currentOrganizationId`)
  3. Body запроса (`request.body.organizationId`)
- Проверка участия пользователя в организации
- Проверка роли в организации (если указана в декораторе)
- Добавление объекта `organizationUser` в `request`

**Использование:**
- Декоратор `@OrganizationAuthorization(OrganizationRole.ADMIN)` для операций, требующих определенной роли в организации
- Декоратор `@OrganizationAuthorization()` для операций, требующих только участия в организации

---

### 5.2. Декораторы

#### @Authorization(roles?)
**Назначение:** Применение авторизации и проверки системных ролей  
**Параметры:**
- `roles` (опционально) - Массив системных ролей (UserRole)

**Использование:**
```typescript
@Authorization() // Требует только авторизации
@Authorization(UserRole.ADMIN) // Требует роль ADMIN
```

---

#### @OrganizationAuthorization(roles?)
**Назначение:** Применение авторизации и проверки ролей в организации  
**Параметры:**
- `roles` (опционально) - Массив ролей в организации (OrganizationRole)

**Использование:**
```typescript
@OrganizationAuthorization() // Требует только участия в организации
@OrganizationAuthorization(OrganizationRole.ADMIN) // Требует роль ADMIN в организации
```

---

#### @Authorized(property)
**Назначение:** Извлечение данных из объекта пользователя в request  
**Параметры:**
- `property` - Название свойства (например, 'userId', 'email')

**Использование:**
```typescript
@Authorized('userId') userId: string
```

---

## 📧 6. Email-уведомления

### 6.1. Отправка писем
**Модуль:** MailModule  
**Сервис:** MailService

**Функционал:**
- Отправка писем через SMTP (Resend.com или обычный SMTP)
- Использование React Email для шаблонов
- Поддержка HTML-шаблонов

**Типы писем:**
- Письмо подтверждения email с токеном

**Конфигурация:**
- Настройки SMTP через переменные окружения
- Поддержка Resend.com и обычных SMTP серверов

---

## 💾 7. База данных (Prisma Schema)

### 7.1. Основные модели

#### User (Пользователи)
**Поля:**
- `userId` - UUID (первичный ключ)
- `email` - Уникальный email
- `passwordHash` - Хеш пароля
- `firstName`, `lastName`, `middleName` - ФИО
- `role` - Роль (ADMIN/REGULAR)
- `isVerified` - Подтвержден ли email
- `isTwoFactorEnabled` - Включена ли 2FA
- `createdAt`, `updatedAt`, `lastLogin` - Даты

**Связи:**
- Многие-ко-многим с Organization через Organization_Users

---

#### Organization (Организации)
**Поля:**
- `organizationId` - UUID (первичный ключ)
- `name` - Название организации
- `createdBy` - ID создателя (user_id)
- `createdAt` - Дата создания

**Связи:**
- Один-ко-многим с Organization_Users
- Один-ко-многим с Warehouses
- Один-ко-многим с Products
- Один-ко-многим с Documents
- Один-ко-многим с Devices

---

#### Organization_Users (Связь пользователей и организаций)
**Поля:**
- `organizationId` - ID организации
- `userId` - ID пользователя
- `role` - Роль в организации (ADMIN/WAREHOUSE_WORKER)

**Составной ключ:** `[organizationId, userId]`

---

#### Warehouses (Склады)
**Поля:**
- `warehouseId` - UUID (первичный ключ)
- `organizationId` - ID организации
- `name` - Название склада
- `address`, `city`, `state`, `zip`, `country` - Адрес
- `status` - Статус (ACTIVE/INACTIVE)
- `createdAt` - Дата создания

**Связи:**
- Многие-к-одному с Organization

---

#### Tokens (Токены)
**Поля:**
- `id` - UUID (первичный ключ)
- `email` - Email пользователя
- `token` - Уникальный токен (UUID)
- `type` - Тип токена (VERIFICATION/PASSWORD_RESET/TWO_FACTOR)
- `expiresIn` - Срок действия

**Назначение:**
- Хранение токенов для подтверждения email, сброса пароля, 2FA

---

### 7.2. Дополнительные модели (заготовки для будущего функционала)

#### Products (Товары)
- Связаны с организациями
- Поля: `productId`, `organizationId`, `name`, `sku`, `barcode`

#### Documents (Документы)
- Связаны с организациями и складами
- Поля: `documentId`, `organizationId`, `warehouseId`, `documentStatus`, `createdBy`

#### Devices (Устройства)
- Связаны с организациями
- Поля: `deviceId`, `organizationId`, `deviceType`, `deviceName`, `qrToken`, `assignedTo`

#### Scan_events (События сканирования)
- Связаны с документами, товарами, пользователями и устройствами
- Поля: `scanEventId`, `documentId`, `productId`, `userId`, `deviceId`, `quantityDelta`

---

## 🔄 8. Сессии и состояние

### 8.1. Express Session
**Хранилище:** Redis  
**Конфигурация:**
- Cookie-based сессии
- Хранение в Redis для масштабируемости
- Настройка через переменные окружения

**Данные в сессии:**
- `userId` - ID авторизованного пользователя
- `currentOrganizationId` - ID текущей выбранной организации

**Особенности:**
- Сессия сохраняется между запросами через cookie
- Автоматическая отправка cookie клиентом
- Настройка домена, безопасности, времени жизни через переменные окружения

---

## 🛠️ 9. Технические особенности

### 9.1. Валидация данных
**Библиотека:** class-validator  
**Использование:**
- DTO (Data Transfer Objects) для всех входящих данных
- Автоматическая валидация через ValidationPipe
- Кастомные валидаторы (например, проверка совпадения паролей)

---

### 9.2. Обработка ошибок
**Типы ошибок:**
- `400 Bad Request` - Ошибки валидации
- `401 Unauthorized` - Пользователь не авторизован
- `403 Forbidden` - Недостаточно прав
- `404 Not Found` - Ресурс не найден
- `409 Conflict` - Конфликт (например, email уже существует)
- `500 Internal Server Error` - Внутренняя ошибка сервера

**Формат ошибок:**
```json
{
  "statusCode": 400,
  "message": "Описание ошибки",
  "error": "Error Type"
}
```

---

### 9.3. Транзакции
**Использование:**
- Prisma транзакции для атомарных операций
- Создание организации + добавление пользователя
- Удаление организации + удаление связей

---

## 📊 10. Статистика функционала

### Реализованные эндпоинты:
- **Аутентификация:** 4 эндпоинта
- **Пользователи:** 2 эндпоинта
- **Организации:** 5 эндпоинтов
- **Склады:** 1 эндпоинт

**Всего:** 12 эндпоинтов

### Модели базы данных:
- **Активно используются:** 5 моделей (User, Organization, Organization_Users, Warehouses, Tokens)
- **Заготовки для будущего:** 7 моделей (Products, Documents, Devices, и др.)

### Guards и декораторы:
- 3 типа guards
- 4 основных декоратора

---

## 🚀 11. Готовность к развертыванию

### Docker поддержка:
- Dockerfile для сборки образа приложения
- docker-compose.yml для запуска всего стека:
  - PostgreSQL база данных
  - Redis для сессий
  - Приложение NestJS

### Конфигурация:
- Переменные окружения для всех настроек
- Поддержка development и production режимов
- Настройка через ConfigModule

---

## 📝 12. Документация

### API_DOCUMENTATION.md
**Содержание:**
- Полное описание всех эндпоинтов
- Примеры запросов и ответов
- Описание всех возможных ошибок
- Типичные сценарии использования
- Рекомендации для разработчика клиента

---

## ✅ Итоговая сводка

### Что реализовано:
✅ Полная система аутентификации (регистрация, логин, логаут)  
✅ Подтверждение email через токены  
✅ Управление пользователями  
✅ Управление организациями (CRUD)  
✅ Выбор текущей организации через сессию  
✅ Создание складов для организаций  
✅ Система ролей (системные и в организациях)  
✅ Guards для защиты эндпоинтов  
✅ Email-уведомления  
✅ Валидация данных  
✅ Docker конфигурация  
✅ Полная API документация  

### Что подготовлено для будущего:
📋 Модели для товаров (Products)  
📋 Модели для документов (Documents)  
📋 Модели для устройств (Devices)  
📋 Модели для событий сканирования (Scan_events)  
📋 Модели для номенклатуры  

---

**Проект готов к использованию базового функционала и расширению дополнительными возможностями.**

